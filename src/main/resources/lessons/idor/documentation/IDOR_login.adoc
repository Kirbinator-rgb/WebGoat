=== Authentication and Authorization

Many access control issues are susceptible to attack from an authenticated-but-unauthorized user. Authentication must rely on the application's established user session rather than a separate shared lesson account.

The legacy shared credentials have been disabled.
